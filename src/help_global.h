#pragma once
#include <string>

class HelpGlobal {
public:
    /**
     * @brief Returns a comprehensive help string describing the application's purpose, behavior, and usage
     * @return Formatted help string with application information
     */
    static std::string help_global();
};
